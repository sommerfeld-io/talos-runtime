---
apiVersion: v1
kind: ConfigMap
metadata:
  name: dashboard-alloy-controller
  labels:
    grafana_dashboard: "1"
data:
  alloy-controller.json: |-
    {{ .Files.Get "dashboards/alloy-controller.json" | toJson | indent 4 }}
