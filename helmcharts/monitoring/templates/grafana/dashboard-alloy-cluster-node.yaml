---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: dashboard-alloy-cluster-node
spec:
  folderRef: folder-alloy
  instanceSelector:
    matchLabels:
      # dashboards: "grafana"
      app.kubernetes.io/instance: {{ .Values.namespaceOverride | default .Release.Name }}
      app.kubernetes.io/name: grafana
  json:
{{ .Files.Get "dashboards/alloy-cluster-node.json" | toJson | indent 4 }}
